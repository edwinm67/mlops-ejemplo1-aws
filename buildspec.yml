version: 0.2

phases:
  install:
    commands:
      - echo Installing dependencies...
      - npm install
  build:
    commands:
      - echo Building the project...
      - npm run build
  post_build:
    commands:
      - echo Running tests...
      - npm test

artifacts:
  files:
    - dist/**/*
    - coverage/**/*
